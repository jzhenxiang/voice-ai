{
  "stt": {
    "preservePrefix": "microphone.",
    "parameters": [
      {
        "key": "listen.model",
        "label": "Model",
        "type": "dropdown",
        "required": true,
        "default": "slam-1",
        "data": "speech-to-text-models.json",
        "valueField": "id",
        "errorMessage": "Please provide valid model for speech to text."
      },
      {
        "key": "listen.language",
        "label": "Language",
        "type": "dropdown",
        "required": true,
        "default": "en",
        "data": "languages.json",
        "valueField": "code",
        "errorMessage": "Please provide valid language for speech to text."
      },
      {
        "key": "listen.threshold",
        "label": "Transcript Confidence Threshold",
        "type": "slider",
        "default": "0.5",
        "min": 0.1,
        "max": 0.9,
        "step": 0.1,
        "helpText": "Transcripts with a confidence score below this threshold will be filtered out."
      }
    ]
  }
}
